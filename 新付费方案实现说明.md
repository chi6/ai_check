# 新付费方案实现说明

## 概述

已成功实现4种新的付费方案，满足不同用户需求：

### 套餐列表

1. **1次查重** (detect_once) - ¥2.00
   - 单次文本查重检测
   - 适合临时需求用户

2. **1次AI查询+查重** (ai_detect_once) - ¥9.00
   - 单次AI检测+文本查重
   - 完整的AI检测服务

3. **1天不限次** (unlimited_1day) - ¥41.00 🔥热门
   - 24小时内不限次数使用（查重+AI）
   - 适合短期密集使用

4. **1周不限次** (unlimited_1week) - ¥87.00 💎超值
   - 7天内不限次数使用（查重+AI）
   - 性价比最高，适合持续使用

## 技术实现

### 1. 数据库修改

#### Orders表新增字段
- `package_type` (TEXT): 套餐类型标识
  - detect_once
  - ai_detect_once
  - unlimited_1day
  - unlimited_1week

#### Licenses表新增字段
- `unlimited` (BOOLEAN): 是否为不限次数套餐
  - True: 不限次数套餐，使用时不扣除额度
  - False: 限次套餐，正常扣除额度

### 2. 后端修改

#### 文件列表
- `app/schemas/database_models.py` - 数据库模型
- `app/services/license_service.py` - License服务逻辑
- `app/routers/pay.py` - 支付接口
- `app/routers/notify.py` - 支付回调
- `app/routers/detect.py` - 额度扣除逻辑
- `migrate_add_package_fields.py` - 数据库迁移脚本

#### 核心逻辑

**License发放**
```python
issue_license(
    db, 
    credits=套餐额度,
    user_id=用户ID,
    unlimited=是否不限次,
    days_valid=有效天数
)
```

**额度消耗**
- 不限次数套餐：不扣除额度，仅记录使用日志
- 限次套餐：正常扣除额度

**优先级规则**
1. 优先使用不限次数套餐（如果有且未过期）
2. 按过期时间排序使用限次套餐（先过期的先用）

### 3. 前端修改

#### TopupModal.jsx
- 更新套餐列表UI，展示4种方案
- 美化卡片样式，添加热门/超值标签
- 修改API调用，传递packageType而非credits/amount

### 4. API接口

#### 新增接口
```
GET /api/pay/packages
返回所有可用套餐配置
```

#### 修改接口
```
POST /api/pay/create_order
请求参数: { channel, packageType, deviceId }
```

## 使用流程

### 购买流程
1. 用户打开充值弹窗
2. 选择套餐类型（4选1）
3. 选择支付渠道（微信/支付宝）
4. 生成订单并展示支付二维码
5. 用户扫码支付
6. 后端接收支付回调
7. 根据套餐类型发放对应License
8. 前端轮询订单状态，支付成功后更新UI

### 使用流程
1. 用户发起检测请求
2. 系统检查用户额度
3. 优先使用不限次数套餐（如果有）
4. 否则使用限次套餐并扣除1次额度
5. 执行检测任务
6. 返回检测结果

## 数据库迁移

已执行迁移脚本添加新字段：
```bash
python3 migrate_add_package_fields.py
```

迁移内容：
- ✅ orders.package_type 字段已添加
- ✅ licenses.unlimited 字段已添加

## 测试

提供测试脚本 `test_new_packages.py` 用于验证功能：
- 测试4种套餐的License创建
- 测试不限次数套餐的额度不扣除
- 测试用户总额度统计
- 测试优先级规则

## 兼容性

### 向后兼容
- 保留旧的API参数（credits、amount）支持旧客户端
- 旧数据（package_type为NULL）仍可正常使用
- 数据库迁移脚本支持重复运行

### 前向扩展
- 套餐配置集中管理（PACKAGES字典）
- 易于添加新套餐类型
- 支持动态套餐价格调整

## 配置说明

套餐配置位于 `app/routers/pay.py`:

```python
PACKAGES = {
    "detect_once": {
        "name": "1次查重",
        "credits": 1,
        "amount": 2.0,
        "unlimited": False,
        "days_valid": None,
        "description": "单次文本查重检测"
    },
    # ... 其他套餐
}
```

修改套餐只需更新此配置，前后端自动同步。

## 注意事项

1. **时区问题**: 所有时间使用UTC时间存储
2. **并发控制**: 额度扣除使用数据库锁（with_for_update）
3. **额度计算**: 不限次数套餐总额度显示为999999
4. **过期检查**: 每次使用时检查License是否过期
5. **支付回调**: 幂等性处理，重复回调不会重复发放

## 部署清单

- [x] 数据库迁移脚本已执行
- [x] 后端代码已更新
- [x] 前端代码已更新
- [x] API文档已更新（本文档）
- [x] 测试脚本已提供

## 后续优化建议

1. 添加套餐购买统计
2. 实现优惠券/折扣码功能
3. 添加套餐使用情况分析
4. 支持套餐续费提醒
5. 实现套餐组合购买

---

实现日期: 2025-10-19
版本: v1.0

